# 🚀 クイックコマンド集（コピペで動く）

## 1️⃣ PC のIPアドレスを確認

### **Windows PowerShell（推奨）**
```powershell
ipconfig | find "IPv4"
```

**実行結果例：**
```
IPv4 アドレス . . . . . . . . . . . . : 192.168.1.100
```

→ `192.168.1.100` があなたの **IPアドレス**

---

### **Windows コマンドプロンプト**
```cmd
ipconfig
```

**実行結果例：**
```
IPv4 アドレス . . . . . . . . . . . . : 192.168.1.100
```

---

### **Mac ターミナル**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**実行結果例：**
```
inet 192.168.1.100 netmask 0xffffff00 broadcast 192.168.1.255
```

→ `192.168.1.100` があなたの **IPアドレス**

---

### **Linux ターミナル**
```bash
hostname -I
```

**実行結果例：**
```
192.168.1.100
```

---

## 2️⃣ PC でサーバーを起動

### **すべての環境共通**

```bash
cd /workspaces/anki-system
python3 -m http.server 8000
```

**成功時の表示：**
```
Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

**❌ このままターミナルは閉じないでください**

---

## 3️⃣ Android + Brave でアクセス

### **URL の形式**
```
http://[あなたのIPアドレス]:8000
```

### **具体例**
```
http://192.168.1.100:8000
http://192.168.0.50:8000
http://10.0.0.15:8000
```

### **手順**
```
1. Brave を開く
2. URL入力欄をタップ
3. http://192.168.1.100:8000 を入力
   （自分のIPアドレスに置き換える）
4. 検索ボタンをタップ
5. 3-5秒待つ
6. アプリが表示される
```

---

## 4️⃣ PWA インストール（Brave）

```
1. Brave でアプリが開いている
2. 画面右下の「⋮」をタップ
3. 「アプリをインストール」をタップ
4. 「インストール」をタップ
```

---

## 🔍 トラブル時の確認コマンド

### **「接続できない」場合**

**Windows:**
```powershell
ping 8.8.8.8
```
→ 応答が返ってくれば WiFi OK

**Mac/Linux:**
```bash
ping 8.8.8.8
```

---

### **サーバーが起動しているか確認（Windows）**

```powershell
netstat -ano | findstr :8000
```

→ 結果が出ればサーバー起動中

---

### **サーバーが起動しているか確認（Mac）**

```bash
lsof -i :8000
```

→ 結果が出ればサーバー起動中

---

## 📝 記録用テンプレート

自分の情報をここに記入：

```
【PC の情報】
OS：________________ (Windows/Mac/Linux)
IPアドレス：192.168.___.___ 
ポート番号：8000
URL：http://192.168.___.___ :8000

【WiFi の情報】
接続している WiFi 名：_________________

【スマホの情報】
OS：Android
ブラウザ：Brave
デバイス：_________________
```

---

## ⚡ よくあるコピペ間違い

### ❌ 間違った入力例
```
http://localhost:8000           ← スマホでは使えません
http://192.168.1.100            ← :8000 を忘れている
https://192.168.1.100:8000      ← https は使わない
192.168.1.100:8000              ← http:// を忘れている
```

### ✅ 正しい入力例
```
http://192.168.1.100:8000       ← 正解！
http://10.0.0.15:8000           ← 正解！
```

---

このガイドで解決できない場合は、「ANDROID_BRAVE_GUIDE.md」を確認してください！
