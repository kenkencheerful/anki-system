# CSVファイル読み込み問題の解決方法

## 問題の原因

ユーザーの `patent_law_1-80v2.csv` がファイル選択画面に表示されない原因は以下の可能性があります：

1. **ファイル形式が実際には CSV ではない**
   - Excel（.xlsx）で保存されている
   - テキストファイル（.txt）として保存されている
   - 他の形式で保存されている

2. **Brave ブラウザのファイルピッカー設定**
   - 「CSVファイル」フィルターで表示がフィルタリングされている
   - ファイル拡張子が正しく認識されていない

3. **ファイルの文字コード**
   - UTF-8 ではなく Shift-JIS で保存されている
   - ブラウザが正しく認識できない形式

---

## 解決策（3段階）

### ステップ 1️⃣ アプリを最新版に更新

**アプリにテキスト直接入力オプションを追加しました。** ファイル選択がうまくいかない場合、以下の方法で CSV を入力できます：

**スマホで実行する手順：**
1. アプリを開く
2. 「保存されたデータ」タブで「📝 テキスト貼り付けで読み込み」をタップ
3. テキスト入力エリアが表示される
4. PC でテキストエディタで CSV を開く
5. 全内容をコピー（Ctrl+A → Ctrl+C）
6. スマホのテキスト入力エリアに貼り付け（長押し → ペースト）
7. 「✓ テキストからインポート」をタップ

**メリット：**
- ファイルピッカーを使わない
- Brave のフィルタ設定の影響を受けない
- 確実に CSV が読み込まれる

---

### ステップ 2️⃣ ファイル形式を修正（PC 側）

万が一テキスト入力方式がうまくいかない場合、PC 側でファイルを正しい形式に変換してください。

#### Windows ユーザー：
1. `patent_law_1-80v2.csv` をメモ帳で開く
   - 右クリック → プログラムから開く → メモ帳

2. ファイル → 名前を付けて保存

3. 以下を設定：
   - ファイル名：`patent_law_1-80v2.csv`
   - ファイルの種類：**「すべてのファイル (*.*)」**
   - 文字コード：**「UTF-8」**
   - 保存

#### Mac ユーザー：
1. `patent_law_1-80v2.csv` をテキストエディットで開く

2. 書類 → オプション
   - エンコーディング：**「UTF-8」** に変更

3. Command + S で保存

#### Chrome ユーザー（参考）：
- 最初からファイルピッカーが「すべてのファイル」で表示されることが多い
- この場合は Brave でも同じ操作で解決する

---

### ステップ 3️⃣ Brave の設定確認

**Brave のファイルピッカーでファイルが見えない場合：**

1. 「📊 CSV形式でインポート」をタップ
2. ファイル選択画面が表示されたら、**右上のフィルター icon** をタップ
3. 「すべてのファイル」に変更
4. CSV ファイルが見えるようになったら選択

---

## 推奨される解決方法

### 最も簡単：テキスト貼り付け（推奨 ⭐️）

1. PC でテキストエディタで `patent_law_1-80v2.csv` を開く
2. Ctrl+A（全選択）→ Ctrl+C（コピー）
3. スマホのアプリ → 「📝 テキスト貼り付けで読み込み」
4. テキスト入力エリアに「長押し → ペースト」
5. 「✓ テキストからインポート」をタップ

**メリット：**
- ファイル形式を気にしなくていい
- Brave の設定に左右されない
- 失敗する可能性が低い

---

## データの確認リスト

CSV が正しいか確認する方法：

```
□ 1. テキストエディタで開いたときに、最初の行に以下が含まれているか
     「カテゴリ」「タイプ」「タイトル」「内容」など

□ 2. 2行目以降にデータが入っているか

□ 3. データはカンマで区切られているか（例：特許法,条文,第1条,内容）

□ 4. 特殊文字や日本語が文字化けしていないか

□ 5. 複数行のテキストがダブルクォートで囲まれているか
     例）"これは
     複数行の
     内容です"
```

---

## トラブルシューティング

| 症状 | 原因 | 解決方法 |
|------|------|---------|
| テキスト入力後にエラーが出る | CSV の形式が正しくない | `sample-template.csv` と比較 |
| データが文字化けしている | 文字コードが UTF-8 ではない | テキストエディタで UTF-8 に変更 |
| 「ヘッダー行とデータ行が必要」というエラー | 1行以下のデータ | データを追加 |
| インポートされたデータが表示されない | タブを切り替える必要がある | 「データ管理」タブ→「学習」タブで確認 |

---

## サンプル CSV で動作確認

まず付属の `sample-template.csv` でテストしてみてください：

1. アプリを開く
2. 「📋 CSVテンプレートをダウンロード」をタップ
3. ダウンロードされたファイルを使用して、テキスト入力方式でインポート
4. 成功したら、あなたのファイルでも同じ方法で実行

---

## ファイルが見つからない場合

**よくある原因：**
- ファイルが PC のダウンロードフォルダにない
- ファイル名が `patent_law_1-80v2.csv` でなく、別の名前で保存されている
- ファイルが削除されてしまった

**確認方法：**
```
Windows：エクスプローラー → ダウンロード フォルダで検索
Mac：Finder → ダウンロードフォルダで検索
Android：ファイルマネージャー → ダウンロードフォルダ
```

---

## 最後の手段：GitHub Pages にデプロイ

ローカルでうまくいかない場合は、GitHub Pages にデプロイして、クラウド上でアプリを使用できます：

1. **GitHub Pages にデプロイ**（詳細は `GITHUB_PAGES_DEPLOYMENT.md` を参照）
2. オンライン URL でアプリを開く
3. テキスト入力方式で CSV をインポート

このほうが安定して動作する場合が多いです。

---

## 質問がある場合

以下の情報があると、より正確なサポートが可能です：

1. **アプリはどこでテストしていますか？**
   - ローカルホスト（localhost:8000）
   - GitHub Pages
   - Netlify

2. **テキスト入力方式でエラーが出た場合、エラーメッセージを教えてください**

3. **CSV ファイルの最初の行をコピーして教えてください**

4. **ブラウザは Brave ですか、それとも他のブラウザですか？**
