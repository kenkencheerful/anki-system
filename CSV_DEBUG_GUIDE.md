# CSVファイル読み込み障害デバッグガイド

## 症状
ファイル選択画面は表示されるが、CSVファイルが選択できない

---

## 【解決方法①】ファイル形式の確認・修正（最初に試す）

### Windowsの場合
1. ファイルエクスプローラーで `patent_law_1-80v2.csv` を右クリック
2. 「プロパティ」を開く
3. **「ファイルの種類」が「CSV ファイル」または「テキスト」になっているか確認**
   - ❌ 「Microsoft Excel」や「xlsx」の場合は形式が間違っています
4. 必要に応じてExcelから再度エクスポート：
   - Excel開く → ファイル → 名前を付けて保存
   - ファイル形式：**「CSV (カンマ区切り) (*.csv)」** を選択
   - 文字コード：**「UTF-8」** を選択
   - 保存

### Macの場合
1. Finderで `patent_law_1-80v2.csv` を選択
2. Cmd + I で情報を開く
3. 「ファイルの種類」が「CSV」になっているか確認
4. 必要に応じてNumbersやExcelから再度エクスポート（UTF-8指定）

### Androidで確認する方法
1. Braveで「📊 CSV形式でインポート」をタップ
2. ファイル選択画面で「すべてのファイル」に切り替える
   - 右上のフィルター icon をタップ
   - 「すべてのファイル」を選択
3. CSVファイルが見えるか確認
4. 見えたら、そのファイルを選択して読み込み

---

## 【解決方法②】別の方法でファイルをアップロード

アプリを GitHub Pages にデプロイしている場合、以下の方法でも可能です：

### テキストエディタでCSVをコピペ
1. PC でテキストエディタ（メモ帳など）で `patent_law_1-80v2.csv` を開く
2. 全選択（Ctrl+A）→ コピー（Ctrl+C）
3. スマホのBrave で アプリを開く
4. 「📊 CSV形式でインポート」下の **テキスト入力エリア**に貼り付け
5. 「インポート」をタップ

※ テキスト入力エリアがない場合は、以下で追加します

---

## 【解決方法③】ファイルエンコーディング（文字コード）を修正

**原因：** CSVファイルが UTF-8 ではなく Shift-JIS で保存されている可能性

### Windowsの場合（テキストエディタで修正）
1. `patent_law_1-80v2.csv` をメモ帳で開く
   - 右クリック → プログラムから開く → メモ帳
2. ファイル → 名前を付けて保存
3. 文字コード：**「UTF-8」** に変更
4. ファイル名：`patent_law_1-80v2.csv` のまま
5. 保存

### Macの場合（テキストエディタで修正）
1. TextEditで `patent_law_1-80v2.csv` を開く
2. 形式 → テキスト形式に変換
3. 書類 → オプション
4. エンコーディング：**「UTF-8」** に変更
5. 保存

---

## 【解決方法④】ファイルの内容確認

### テキストエディタで最初の数行を確認
1. メモ帳で開く
2. **最初の行（ヘッダー行）に以下が含まれているか確認：**
   - `カテゴリ` または `category`
   - `タイプ` または `type`
   - `タイトル` または `title`
   - `内容` または `detail`

3. **2行目以降にデータが正しく入っているか確認**
   - カンマが正しく区切られている
   - ダブルクォートで囲まれた部分に改行やカンマが含まれていないか
   - 特殊文字が文字化けしていないか

### サンプルで動作確認
1. 最初に付属の `sample-template.csv` でテストしてみてください
2. 動作確認できたら、あなたのCSVファイルと比較して原因を特定

---

## 【解決方法⑤】テキスト入力フォームの活用（推奨）

現在のアプリをアップデートして、テキスト直接入力オプションを追加しました：

1. ファイル選択がうまくいかない場合、以下の代替方法が使えます：
   - PC でテキストエディタで CSV を開く
   - 全内容をコピー
   - スマホのアプリで「テキスト貼り付けオプション」に貼り込み
   - インポート

---

## よくある問題と解決策

| 問題 | 原因 | 解決策 |
|------|------|-------|
| ファイルピッカーに CSV が表示されない | Braveのフィルタ設定 | 「すべてのファイル」に変更 |
| CSV が選択できない（グレーアウト） | 形式が CSV ではない | Excel → CSV でエクスポート |
| インポート後にエラー | 文字コードが UTF-8 ではない | メモ帳で UTF-8 に変更 |
| データが文字化けしている | 文字コードが Shift-JIS | UTF-8 に変更して再保存 |
| 「ファイルが認識できない」というエラー | CSV の構造が異なる | `sample-template.csv` と比較 |

---

## 確認チェックリスト

```
□ 1. ファイル拡張子が .csv である
□ 2. ファイル形式が「CSV ファイル」（テキストファイル）である
□ 3. 文字コードが UTF-8 である
□ 4. 最初の行にヘッダーがある（カテゴリ、タイプ、タイトル、内容など）
□ 5. 2行目以降にデータが入っている
□ 6. カンマ区切りが正しい
□ 7. Braveで「すべてのファイル」フィルタを使用している
□ 8. アプリを更新して最新版を使用している
```

---

## 次のステップ

上記の手順を試しても解決しない場合は、以下の情報を教えてください：

1. **ファイル拡張子確認**
   - Braveで「すべてのファイル」にしてファイルが見えたか

2. **ファイル形式確認**
   - Windows なら プロパティの「ファイルの種類」
   - Mac なら Cmd + I の「ファイルの種類」

3. **CSVの最初の行を教えてください**
   - コピーして貼り付けていただけると、原因を特定しやすいです

4. **エラーメッセージの画面スクリーンショット**
   - あれば教えてください
