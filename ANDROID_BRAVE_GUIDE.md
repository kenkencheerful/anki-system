# 📱 Android + Brave ブラウザでのインストール完全ガイド

## ステップ1️⃣：PCのIPアドレスを確認

### **Windows の場合**

#### 方法A：PowerShell（推奨・簡単）
```
1. Windowsキー + R を押す
2. 「powershell」と入力して Enter
3. 以下のコマンドをコピー＆ペースト：
   
   ipconfig | find "IPv4"
   
4. Enter を押す
5. 「IPv4 アドレス」を確認
   例：192.168.1.100
```

#### 方法B：コマンドプロンプト
```
1. Windowsキー + R を押す
2. 「cmd」と入力して Enter
3. 以下をコピー＆ペースト：
   
   ipconfig
   
4. Enter を押す
5. 「IPv4 アドレス」を確認
```

#### 方法C：設定画面から確認
```
1. Windows スタートメニュー
2. 「設定」をクリック
3. 「ネットワークとインターネット」
4. 「WiFi」または「イーサネット」をクリック
5. 「詳細オプション」をクリック
6. 「IPv4 アドレス」を確認
```

**✅ IPアドレスの例**
```
192.168.1.100  ← よくあるパターン
192.168.0.50
10.0.0.15
```

---

### **Mac の場合**

#### 方法A：ターミナル（推奨）
```
1. Spotlight検索（cmd + Space）で「ターミナル」を検索
2. ターミナルを開く
3. 以下のコマンドをコピー＆ペースト：
   
   ifconfig | grep "inet " | grep -v 127.0.0.1
   
4. Enter を押す
5. 「inet」の後の数字がIPアドレス
   例：inet 192.168.1.100
```

#### 方法B：システム設定から確認
```
1. 画面左上のAppleメニュー
2. 「システム設定」をクリック
3. 左メニューの「ネットワーク」をクリック
4. 「WiFi」または「Ethernet」をクリック
5. 「詳細情報」をクリック
6. 「IPv4 アドレス」を確認
```

---

### **Linux の場合**

```
ターミナルで以下のコマンド：

hostname -I

または

ifconfig | grep "inet " | grep -v 127.0.0.1
```

---

## ステップ2️⃣：PC でローカルサーバーを起動

### **ターミナル/コマンドプロンプルを開く**

#### Windows PowerShell
```
1. Windowsキー + R
2. 「powershell」と入力 → Enter
3. 以下をコピー＆ペースト：

   cd /workspaces/anki-system
   python3 -m http.server 8000

4. Enter を押す
5. 以下のような表示が出れば OK：
   
   Serving HTTP on 0.0.0.0 port 8000 ...
```

#### Mac/Linux ターミナル
```
1. ターミナルを開く
2. 以下をコピー＆ペースト：

   cd /workspaces/anki-system
   python3 -m http.server 8000

3. Enter を押す
4. 以下のような表示が出れば OK：
   
   Serving HTTP on 0.0.0.0 port 8000 ...
```

**⚠️ ここでは ターミナル/PowerShell を閉じないでください！**

---

## ステップ3️⃣：AndroidスマホとPCが同じWiFiに接続

### **スマホの WiFi 確認**

```
1. Android スマホを開く
2. 「設定」をタップ
3. 「ネットワークとインターネット」をタップ
4. 「WiFi」をタップ
5. 現在接続している WiFi 名を確認
   例：「Buffalo-5G」「home-wifi」など
```

### **PC の WiFi 確認**

```
Windows：
  右下のWiFiアイコン → 接続している WiFi 名を確認

Mac：
  画面右上のWiFiアイコン → 接続している WiFi 名を確認
```

**✅ スマホと PC の WiFi 名が同じなら OK**

---

## ステップ4️⃣：Brave ブラウザでアプリを開く

### **Brave ブラウザをインストール（未インストール場合）**

```
Google Play Store から：
1. Google Play Store を開く
2. 検索欄に「Brave」と入力
3. 「Brave Private Web Browser」をタップ
4. 「インストール」をタップ
5. インストール完了後、「開く」をタップ
```

### **URL を入力**

```
1. Brave ブラウザを開く
2. URL入力欄をタップ
3. 以下を入力（IPアドレスは自分のものに置き換え）：

   http://192.168.1.100:8000
   
   例：
   - http://192.168.1.100:8000
   - http://192.168.0.50:8000
   - http://10.0.0.15:8000

4. 虫眼鏡ボタン（検索）をタップ
```

**⚠️ 重要ポイント**
- **http:// を必ず含める**（https ではなく http）
- **:8000 を忘れずに**
- IPアドレスは半角数字とドットのみ

### **アプリが表示されるまで待つ**

```
1. 少し待つ（3-5秒）
2. 暗いグラデーションの画面に「弁理士試験 暗記アプリ」が表示
3. ✅ 成功！
```

**💡 もし表示されない場合**
- ページを更新（Brave の上部、右から左へスワイプ）
- アドレスが正しいか再確認
- PC のターミナルで `python3 -m http.server 8000` が実行中か確認

---

## ステップ5️⃣：PWA をホーム画面に追加（Brave）

### **インストール方法**

```
1. Brave でアプリが開いている状態
2. 画面右下の「⋮」（メニュー）をタップ
3. 「アプリをインストール」をタップ
   または
   「ホーム画面に追加」をタップ
   
   ※ Brave の バージョンにより表示が異なります
```

### **表示されない場合**

```
1. ブラウザの右上の「⋮」（メニュー）をタップ
2. 下にスクロール
3. 「アプリをインストール」または 
   「Install app」という選択肢を探す
4. タップしてインストール
```

### **確認メッセージが出たら**

```
「このアプリをインストールしますか？」

選択肢：
  「インストール」← これをタップ

アプリ名：「弁理士暗記」
```

**✅ インストール完了！**

---

## ステップ6️⃣：ホーム画面から起動

```
1. Android ホーム画面を開く
2. 「弁理士暗記」というアイコンが追加されている
   紫色の背景に「暗」という文字のアイコン
3. これをタップして起動
4. ✅ アプリが全画面で起動します！
   （ブラウザのアドレスバーが非表示）
```

---

## 🎯 トラブルシューティング

### **問題1：「接続できません」と表示される**

**原因：スマホとPCが同じWiFiに接続していない**

```
解決方法：
1. スマホの設定 → WiFi
2. PC と同じ WiFi に接続
3. もう一度 URL を入力してアクセス

確認：
  スマホと PC のWiFi 名が同じ？
  例：どちらも「home-wifi」に接続している
```

### **問題2：IPアドレスが不明**

**原因：IPアドレスの確認漏れ**

```
再度確認：
【Windows】
  PowerShell で：ipconfig | find "IPv4"
  
【Mac】
  ターミナルで：ifconfig | grep "inet "
  
【確認作業】
  コマンド実行後、「192.168.」や「10.0.」から始まる
  番号が IPアドレス です
```

### **問題3：「アプリをインストール」が表示されない**

**原因：manifest.json が読み込まれていない**

```
解決方法：
1. Brave でページを再読み込み
   （画面上部を下へスワイプ、または F5 キー）
2. 3-5秒待つ
3. もう一度メニュー（⋮）をタップ
```

### **問題4：「localhost」と入力したが動かない**

**原因：スマホは「localhost」が使えません**

```
❌ 間違い：http://localhost:8000
✅ 正解：  http://192.168.1.100:8000
           （自分のPCのIPアドレス）
```

### **問題5：「ページが見つかりません」**

**原因1：ポート番号が違う**
```
確認：PC のターミナルに
  「port 8000」と表示されている？
```

**原因2：ファイルがない**
```
確認：/workspaces/anki-system に
  index.html
  styles.css
  script.js
  manifest.json
  sw.js
  がすべて存在する？
```

**原因3：パス指定が違う**
```
確認：以下の画面が表示される？
  紫色のグラデーション
  「弁理士試験 暗記アプリ」というタイトル
```

---

## ✅ 完全チェックリスト

### **PC での準備**
- [ ] Python 3 がインストールされている
- [ ] `/workspaces/anki-system` ディレクトリに 5つのファイルがある
- [ ] PowerShell/ターミナルで `python3 -m http.server 8000` が実行できる
- [ ] IPアドレスが確認できた
  IPアドレス：`___ . ___ . ___ . ___`

### **Android での準備**
- [ ] WiFi が ONになっている
- [ ] PC と同じ WiFi に接続されている
- [ ] Brave ブラウザがインストールされている

### **接続テスト**
- [ ] URL `http://[IPアドレス]:8000` で アプリが表示される
- [ ] 画面に「弁理士試験 暗記アプリ」が表示される
- [ ] 「学習」タブと「📋 データ管理」タブが見える

### **インストール**
- [ ] Brave のメニュー（⋮）から「アプリをインストール」が表示される
- [ ] 「インストール」をタップして完了
- [ ] ホーム画面に「弁理士暗記」アイコンが追加される

### **確認**
- [ ] ホーム画面のアイコンをタップで起動
- [ ] ブラウザの UI が非表示（全画面表示）
- [ ] ✅ 完全成功！

---

## 🎓 初回起動後の使い方

```
【1日目】
1. 「📋 データ管理」タブ
2. 「📋 CSVテンプレートをダウンロード」
   ※ ダウンロードアプリで確認
3. PC で Excel を開いて編集
4. アプリに戻って「📊 CSV形式でインポート」

【2日目以降】
1. 毎日、ホーム画面のアイコンをタップ
2. 「学習」タブ
3. 「🔴 今日復習」のカード学習
4. 難易度を評価（😰/✓/😊）
5. 次のカードへ自動遷移
```

---

## 💡 よくある質問

**Q. PC のターミナルを閉じてもいい？**
A. いいえ。ターミナルを閉じるとサーバーが停止します。

**Q. スマホをWiFiから外すとどうなる？**
A. オフライン対応なので、2回目以降は WiFi なしで起動できます。

**Q. Brave 以外のブラウザでもできる？**
A. はい。Chrome、Edge、Firefoxでも可能です。

**Q. 別の WiFi に繋いだらどうなる？**
A. サーバーにアクセスできなくなります。同じWiFiに戻してください。

**Q. 自宅外で使いたい**
A. JSON でバックアップを取り、別のサーバーにアップロードしてください（オプション）。

---

## 🚀 成功したら次は？

```
1. ✅ アプリがスマホで起動
2. ✅ 「📋 CSVテンプレート」をダウンロード
3. ✅ 条文・判例を入力
4. ✅ インポート
5. ✅ 毎日学習開始！

頑張ってください！🎓✨
```

---

**質問や問題があればお知らせください！**
